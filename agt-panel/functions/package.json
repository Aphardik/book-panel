{
  "name": "book-interest-functions",
  "description": "Cloud Functions for LibreShare Book Interest System",
  "scripts": {
    "build": "tsc",
    "start": "npm run build && firebase emulator:start --only functions",
    "deploy": "firebase deploy --only functions",
    "logs": "firebase functions:log"
  },
  "main": "lib/index.js",
  "dependencies": {
    "firebase-admin": "^12.0.0",
    "firebase-functions": "^4.4.1"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "typescript": "^5.0.0"
  },
  "engines": {
    "node": "20"
  }
}
